spring:
    application:
        name: taco
    output:
        ansi:
            enabled: ALWAYS
    datasource:
        generate-unique-name: false
        name: taco
        # url: jdbc:h2:taco

server:
    port: 8080
    servlet:
        context-path: /taco